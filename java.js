let i = 0;
const shoppingBasket= [
    "banana",
    "apple",
    "grapes",
    "oranges"
];
while (shoppingBasket.length > 0 ) {
    const item = baskeyt.pop()
    console.log ("putting" + item + "in the checkout belt");
}
    console.log("for next cunśtomer");